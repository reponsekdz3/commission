"use client";import{useEffect,useState}from"react";import{authApi}from"../../lib/api";
export default function Messages(){const[cs,setCs]=useState<any[]>([]);const[active,setActive]=useState<any>();const[body,setBody]=useState("");const[err,setErr]=useState("");async function load(){try{setCs(await authApi<any[]>("/messages/conversations"))}catch(e:any){setErr(e.message)}}useEffect(()=>{load()},[]);async function open(id:string){try{setActive(await authApi<any>("/messages/conversations/"+id));await authApi("/messages/conversations/"+id+"/read",{method:"POST"})}catch(e:any){setErr(e.message)}}async function send(){if(!body.trim()||!active)return;try{await authApi("/messages",{method:"POST",body:JSON.stringify({conversationId:active.id,body})});setBody("");await open(active.id)}catch(e:any){setErr(e.message)}}return <main className="wrap section"><div className="eyebrow">Realtime communication</div><h1>Inbox</h1><p className="muted">Keep property conversations, viewing coordination and transaction context together.</p>{err&&<div className="notice error">{err}</div>}<div className="layout"><div className="panel"><div className="sectionLabel"><b>Conversations</b><span className="muted">{cs.length}</span></div><div className="list">{cs.map((c:any)=><button className="row" style={{textAlign:"left",width:"100%"}} key={c.id} onClick={()=>open(c.id)}><span><b>{c.title||c.otherUserName||c.id}</b><br/><small className="muted">{c.lastMessage||"Open conversation"}</small></span><span>→</span></button>)}</div>{!cs.length&&<div className="empty">No conversations returned by the API.</div>}</div><div className="panel" style={{minHeight:520,display:"flex",flexDirection:"column"}}><div><div className="eyebrow">Conversation</div><h2 style={{fontSize:24}}>{active?.title||"Select a conversation"}</h2></div><div style={{flex:1}}>{active?.messages?.map((m:any)=><div className="row" key={m.id} style={{marginTop:10}}><span>{m.body}</span><small className="muted">{m.senderId===active.userId?"You":"Them"}</small></div>)}</div>{active&&<div className="actions" style={{marginTop:14}}><input className="field" value={body} onChange={e=>setBody(e.target.value)} placeholder="Write a message…"/><button className="btn" onClick={send}>Send →</button></div>}</div></div></main>