"use client";import {useEffect,useState} from "react";import Link from "next/link";import {authApi} from "../../lib/api";
export default function Favorites(){const [items,setItems]=useState<any[]>([]);const [err,setErr]=useState("");useEffect(()=>{authApi<any[]>("/favorites").then(setItems).catch(e=>setErr(e.message))},[]);return <main className="wrap section"><div className="eyebrow">Saved</div><h1>Favorites</h1>{err&&<div className="notice">{err} · <Link href="/login">Sign in</Link></div>}<div className="list">{items.map((x:any)=><Link className="row" key={x.propertyId||x.id} href={`/properties/${x.propertyId||x.id}`}><span>{x.title||x.property?.title||x.propertyId}</span><span>Open →</span></Link>)}</div></main>}
