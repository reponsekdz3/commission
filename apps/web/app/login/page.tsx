"use client";import {useState} from "react";import {apiUrl} from "../../lib/api";import Link from "next/link";
export default function Login(){const [register,setRegister]=useState(false);const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [name,setName]=useState("");const [phone,setPhone]=useState("");const [mfa,setMfa]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:any){e.preventDefault();setBusy(true);setError("");try{const endpoint=register?"/auth/register":"/auth/login";const body=register?{email,phone,fullName:name,password}: {identifier:email,password,...(mfa?{mfaCode:mfa}:{})};const r=await fetch(apiUrl(endpoint),{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.message||d.error||"Request failed");if(!register){localStorage.setItem("imizi_token",d.accessToken);localStorage.setItem("imizi_refresh",d.refreshToken);localStorage.setItem("imizi_user",JSON.stringify(d.user));location.href="/dashboard"}else{setRegister(false);setError("Account created. Sign in to continue.")}}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <main className="wrap form"><div className="eyebrow">Secure account</div><h1>{register?"Create your account":"Welcome back"}</h1><p className="muted">Authentication is handled by the production API with refresh sessions and optional MFA.</p><form className="formGrid" onSubmit={submit}>{register&&<><input className="field" value={name} onChange={e=>setName(e.target.value)} placeholder="Full name" required/><input className="field" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+250..." /></>}<input className="field" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" required/><input className="field" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" required/>{!register&&<input className="field" value={mfa} onChange={e=>setMfa(e.target.value)} placeholder="MFA code (if enabled)" inputMode="numeric" />}{error&&<div className="notice">{error}</div>}<button className="btn" disabled={busy}>{busy?"Working…":register?"Create account":"Sign in"}</button></form><button className="navBtn" style={{marginTop:16}} onClick={()=>{setRegister(!register);setError("")}}>{register?"Already have an account? Sign in":"Need an account? Register"}</button><p><Link className="muted" href="/">← Home</Link></p></main>}
