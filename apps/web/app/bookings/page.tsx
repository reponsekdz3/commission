"use client";import {useEffect,useState} from "react";import {authApi} from "../../lib/api";export default function Bookings(){const [x,setX]=useState<any[]>([]);const [e,setE]=useState("");useEffect(()=>{authApi<any[]>("/bookings").then(setX).catch(e=>setE(e.message))},[]);async function cancel(id:string){try{await authApi(`/bookings/${id}/cancel`,{method:"POST"});setX(await authApi<any[]>("/bookings"))}catch(e:any){setE(e.message)}}return <main className="wrap section"><div className="eyebrow">Transactions</div><h1>Bookings</h1>{e&&<div className="notice">{e}</div>}<div className="list">{x.map(b=><div className="row" key={b.id}><span><b>{b.id}</b><br/><small className="muted">{b.startDate} → {b.endDate}</small></span><div className="actions"><span>{b.status}</span>{b.status!=="CANCELLED"&&<button className="btn ghost" onClick={()=>cancel(b.id)}>Cancel</button>}</div></div>)}</div></main>}
