import Link from "next/link";import{api,formatRwf}from"../lib/api";export const dynamic="force-dynamic";
type R={items:any[]};
export default async function Home(){let data:R={items:[]};try{data=await api<R>("/search?listingType=RENT&limit=8")}catch{}const districts=["Gasabo","Kicukiro","Nyarugenge","Musanze","Rubavu","Huye"];
return <main><section className="wrap hero"><div className="eyebrow">Rwanda-first property marketplace</div><h1>Find a place that fits your life.</h1><p className="lead">Search verified homes, land and commercial spaces. Book viewings, make offers, pay securely and manage your property from one connected platform.</p><form className="searchbox" action="/search"><input className="field" name="q" placeholder="Try “3 bedroom near Kigali CBD”" aria-label="Search properties"/><select className="field" name="listingType" defaultValue="RENT"><option value="RENT">Rent</option><option value="SALE">Buy</option><option value="SHORT_STAY">Short stay</option></select><select className="field" name="verifiedOnly" defaultValue=""><option value="">Any listing</option><option value="true">Verified only</option></select><button className="btn">Search →</button></form><div className="chipRow" style={{marginTop:16}}>{["Rent","Buy","Short stay","Land","Commercial"].map((x,i)=><span className={i===0?"chip active":"chip"} key={x}>{x}</span>)}</div><div className="heroStats"><span>✓ <strong>Verified listings</strong></span><span>⌖ <strong>Map search</strong></span><span>↗ <strong>Real booking & payments</strong></span><span>◉ <strong>RWF native</strong></span></div></section>
<section className="wrap section"><div className="sectionLabel"><div><div className="eyebrow">Explore Rwanda</div><h2>Search by district</h2></div><Link href="/map" className="muted">Open live map →</Link></div><div className="chipRow">{districts.map(x=><Link className="chip" href={"/search?district="+encodeURIComponent(x)} key={x}>{x}</Link>)}</div></section>
<section className="wrap section"><div className="sectionHead"><div><div className="eyebrow">Live inventory</div><h2>Featured homes</h2><div className="muted">Real results returned by the backend search engine.</div></div><Link href="/search" className="btn ghost">View all listings</Link></div><div className="grid">{data.items.map((x:any)=><Link className="card" href={"/properties/"+x.property.id} key={x.listing.id}>{x.property.media?.[0]?.url?<img className="cardImg" src={x.property.media[0].url} alt={x.property.title}/>:<div className="cardImg mediaEmpty">Media unavailable</div>}<div className="cardBody"><span className="pill">{x.property.verificationStatus==="VERIFIED"?"Verified":"Listed"}</span><div style={{fontWeight:800,marginTop:10}}>{x.property.title}</div><div className="meta">{x.property.district} · {x.property.bedrooms??"—"} bd · {x.property.bathrooms??"—"} ba</div><div className="price">{formatRwf(x.listing.priceMinor)}{x.listing.listingType==="RENT"?"/mo":""}</div></div></Link>)}</div>{!data.items.length&&<div className="empty" style={{marginTop:20}}><h3>No live listings yet</h3><p>Connect the production API and publish inventory; this screen intentionally does not fabricate property data.</p><Link className="btn" href="/manage">Manage inventory</Link></div>}</section>
<section className="wrap section"><div className="two"><div className="panel"><div className="eyebrow">For seekers</div><h2>From discovery to keys.</h2><p className="muted">Search, compare, save, message owners, request a viewing, book and pay without losing the property context.</p><div className="actions"><Link className="btn" href="/search">Start exploring</Link><Link className="btn ghost" href="/compare">Compare homes</Link></div></div><div className="panel"><div className="eyebrow">For owners</div><h2>Operate your portfolio.</h2><p className="muted">Create listings, publish when eligible, track bookings, leases, maintenance, payments and performance from the workspace.</p><div className="actions"><Link className="btn" href="/dashboard">Open dashboard</Link><Link className="btn ghost" href="/manage">List a property</Link></div></div></div></section>
<footer className="wrap footer"><strong>IMIZI</strong> · Property discovery, transactions and operations · Rwanda · <Link href="/legal">Legal & privacy</Link></footer></main>}