"use client";import{useEffect,useState}from"react";import Link from"next/link";import{api}from"../../lib/api";import{MapboxClient}from"./mapbox-client";export default function MapPage(){const[items,setItems]=useState<any[]>([]);const[q,setQ]=useState("");useEffect(()=>{api<any>("/search?listingType=RENT&limit=50").then(x=>setItems(x.items||[])).catch(()=>{})},[]);const visible=items.filter(x=>!q||String(x.property.title).toLowerCase().includes(q.toLowerCase()));return <main className="wrap section"><div className="sectionHead"><div><div className="eyebrow">Geospatial discovery</div><h1>Property map</h1><p className="muted">Live listing coordinates are rendered by Mapbox and sourced from the real search API.</p></div><input className="field" style={{maxWidth:320}} value={q} onChange={e=>setQ(e.target.value)} placeholder="Filter listings"/></div><MapboxClient items={visible}/><div className="list">{visible.slice(0,15).map((x:any)=><Link className="row" href={`/properties/${x.property.id}`} key={x.listing.id}><span><b>{x.property.title}</b><br/><small className="muted">{x.property.district}</small></span><span>Open →</span></Link>)}</div></main>}