import "reflect-metadata";import{NestFactory}from"@nestjs/core";import type{NestExpressApplication}from"@nestjs/platform-express";import{RequestMethod,ValidationPipe}from"@nestjs/common";import helmet from"helmet";import{DocumentBuilder,SwaggerModule}from"@nestjs/swagger";import{AppModule}from"./app.module";import{loadConfig}from"@imizi/config";import{HttpExceptionFilter}from"./common/http-exception.filter";
async function bootstrap(){const config=loadConfig();const app=await NestFactory.create<NestExpressApplication>(AppModule,{cors:false,rawBody:true});app.set("trust proxy",process.env.TRUST_PROXY==="true"?1:false);app.disable("x-powered-by");app.use(helmet({crossOriginResourcePolicy:{policy:"cross-origin"}}));
const configured=[process.env.APP_URL,process.env.ADMIN_URL,...(process.env.CORS_ORIGINS||"").split(",")].map(x=>x?.trim()).filter(Boolean) as string[];const origins=process.env.NODE_ENV==="production"?configured:Array.from(new Set([...configured,"http://localhost:3000","http://localhost:3002","http://localhost:8081"]));app.enableCors({origin:(origin,cb)=>{if(!origin||origins.includes(origin))return cb(null,true);return cb(new Error("CORS origin denied"),false)},credentials:true,methods:["GET","HEAD","POST","PUT","PATCH","DELETE","OPTIONS"],allowedHeaders:["Content-Type","Authorization","X-Request-Id","Idempotency-Key"],maxAge:86400});
app.setGlobalPrefix(config.apiPrefix.replace(/^\//,""),{exclude:[{path:"health",method:RequestMethod.GET},{path:"ready",method:RequestMethod.GET},{path:"live",method:RequestMethod.GET},{path:"metrics",method:RequestMethod.GET}]});
app.useGlobalPipes(new ValidationPipe({transform:true,whitelist:true,forbidNonWhitelisted:true,disableErrorMessages:process.env.NODE_ENV==="production"}));app.useGlobalFilters(new HttpExceptionFilter());
const swagger=new DocumentBuilder().setTitle("Imizi API").setDescription("Real estate marketplace, booking, payments, maps, messaging, and property operations.").setVersion("1.0.0").addBearerAuth().build();SwaggerModule.setup("docs",app,SwaggerModule.createDocument(app,swagger),{useGlobalPrefix:false});
await app.listen(config.port,"0.0.0.0")}bootstrap();