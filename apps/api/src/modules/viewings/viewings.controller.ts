import{Body,Controller,Get,Param,Post}from"@nestjs/common";import{ApiBearerAuth,ApiTags}from"@nestjs/swagger";import{CurrentUser}from"../../common/current-user.decorator";import{UserRecord}from"../../store/platform.store";import{FeatureService}from"../../infra/feature.service";import{viewingRequestSchema,viewingDecisionSchema}from"@imizi/validation";
@ApiTags("viewings")@ApiBearerAuth()@Controller("viewings")export class ViewingsController{constructor(private readonly features:FeatureService){}@Get("slots/:listingId")slots(@Param("listingId")id:string){return this.features.viewingSlots(id)}@Post()request(@CurrentUser()u:UserRecord,@Body()b:unknown){const d=viewingRequestSchema.parse(b);return this.features.requestViewing(u.id,d.listingId,d.slotStart)}@Post(":id/decide")decide(@CurrentUser()u:UserRecord,@Param("id")id:string,@Body()b:unknown){return this.features.decideViewing(u.id,id,viewingDecisionSchema.parse(b).accept)}}