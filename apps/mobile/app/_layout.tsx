import{Stack}from"expo-router";import{StatusBar}from"expo-status-bar";import{useEffect}from"react";import*as Notifications from"expo-notifications";import{Platform}from"react-native";import{api}from"../src/lib/api";export default function Root(){useEffect(()=>{(async()=>{if(Platform.OS==="web")return;const p=await Notifications.getPermissionsAsync();let status=p.status;if(status!=="granted"){status=(await Notifications.requestPermissionsAsync()).status}if(status!=="granted")return;try{const projectId=process.env.EXPO_PUBLIC_EAS_PROJECT_ID;if(!projectId)return;const t=(await Notifications.getExpoPushTokenAsync({projectId})).data;await api("/notifications/push-token",{method:"POST",body:JSON.stringify({token:t,platform:Platform.OS==="ios"?"ios":"android"})},true)}catch{}})()},[]);return <><StatusBar style="dark"/><Stack screenOptions={{headerShown:false,contentStyle:{backgroundColor:"#F7F8FA"}}}><Stack.Screen name="(tabs)"/><Stack.Screen name="login"/></Stack></>}