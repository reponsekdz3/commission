import{router}from"expo-router";import{useEffect,useState}from"react";import{FlatList,StyleSheet,Text,View}from"react-native";import{api}from"../../src/lib/api";export default function Saved(){const[x,setX]=useState<any[]>([]);const[e,setE]=useState("");useEffect(()=>{api<any[]>("/favorites",{},true).then(setX).catch(a=>setE(a.message))},[]);return <View style={s.root}><FlatList data={x} keyExtractor={(a:any)=>a.propertyId||a.id} contentContainerStyle={s.pad} ListHeaderComponent={<><Text style={s.h1}>Saved</Text>{e&&<Text style={s.error}>{e}</Text>}</>} renderItem={({item})=><Text style={s.row} onPress={()=>router.push(`/property/${item.propertyId||item.id}`)}>{item.title||item.property?.title||item.propertyId}  ›</Text>}/></View>}const s=StyleSheet.create({root:{flex:1,backgroundColor:"#F7F8FA"},pad:{padding:18},h1:{fontSize:36,fontWeight:"900",marginBottom:18},row:{backgroundColor:"#fff",borderWidth:1,borderColor:"#E5E7EB",borderRadius:12,padding:16,marginBottom:8,fontWeight:"700"},error:{color:"#991B1B",marginBottom:12}})