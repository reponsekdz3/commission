import{useLocalSearchParams,useRouter}from"expo-router";import{useState}from"react";import{Alert,Pressable,SafeAreaView,StyleSheet,Text,TextInput,View}from"react-native";import{api,money}from"../src/lib/api";
function addMonths(date:Date,n:number){const copy=new Date(date);copy.setMonth(copy.getMonth()+n);return copy.toISOString().slice(0,10)}
export default function Booking(){const{listingId}=useLocalSearchParams<{listingId:string}>();const router=useRouter();const today=new Date();const[startDate,setStartDate]=useState(today.toISOString().slice(0,10));const[endDate,setEndDate]=useState(addMonths(today,1));const[busy,setBusy]=useState(false);async function submit(){if(!listingId)return;setBusy(true);try{const quote=await api<any>("/bookings/quote",{method:"POST",body:JSON.stringify({listingId,startDate,endDate})});const b=await api<any>("/bookings",{method:"POST",body:JSON.stringify({listingId,startDate,endDate,guests:1,idempotencyKey:"mobile-"+Date.now()+"-"+Math.random().toString(36).slice(2,18)})},true);const booking=b.booking||b;if(!booking.id)throw new Error(b.message||"Unable to create booking");const pay=await api<any>("/payments/intents",{method:"POST",body:JSON.stringify({bookingId:booking.id,provider:"MTN_MOMO",idempotencyKey:"mobile-pay-"+Date.now()+"-"+Math.random().toString(36).slice(2,18)})},true);Alert.alert("Booking created","Total "+money(quote.total?.amountMinor??quote.amountMinor??0)+". Payment status: "+(pay.status||"initiated"),[{text:"Done",onPress:()=>router.replace("/")}])}catch(e){Alert.alert("Booking failed",e instanceof Error?e.message:"Unable to create booking")}finally{setBusy(false)}}return <SafeAreaView style={styles.safe}><View style={styles.wrap}><Text style={styles.title}>Reserve property</Text><Text style={styles.muted}>Availability, pricing, booking and payment are checked by the production API.</Text><Text style={styles.label}>Start date</Text><TextInput value={startDate} onChangeText={setStartDate} style={styles.input}/><Text style={styles.label}>End date</Text><TextInput value={endDate} onChangeText={setEndDate} style={styles.input}/><Pressable style={styles.button} disabled={busy} onPress={submit}><Text style={styles.buttonText}>{busy?"Processing…":"Quote · book · pay"}</Text></Pressable></View></SafeAreaView>}
const styles=StyleSheet.create({safe:{flex:1,backgroundColor:"#f7f8fa"},wrap:{padding:22,gap:12},title:{fontSize:32,fontWeight:"900"},muted:{color:"#64748b",lineHeight:21},label:{fontWeight:"800",marginTop:8},input:{backgroundColor:"#fff",borderRadius:14,padding:15,borderWidth:1,borderColor:"#e2e8f0",fontSize:16},button:{backgroundColor:"#0f5132",padding:16,borderRadius:14,alignItems:"center",marginTop:10},buttonText:{color:"#fff",fontWeight:"900",fontSize:16}})